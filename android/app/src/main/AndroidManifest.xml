<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- the 15-minute check waits for a connection, and Android 14+ needs this for jobs with a network constraint -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- lets the site buzz the phone (navigator.vibrate) without needing a new APK -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- "your move" notifications for online games (Android 13+ asks the player first) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- so the 15-minute "your move" check carries on after the phone restarts -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <meta-data android:name="asset_statements" android:resource="@string/asset_statements" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Goal Machine links (e.g. a friend's challenge) open in the app; verified by /.well-known/assetlinks.json -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="opportunisticgames.github.io" android:pathPrefix="/goal-machine" />
            </intent-filter>
        </activity>
        <!-- instant notifications pushed by the server through Firebase -->
        <service
            android:name=".PushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".GameCheckService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <provider
            android:name=".ShareProvider"
            android:authorities="${applicationId}.share"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
