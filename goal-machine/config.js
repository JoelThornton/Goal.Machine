// Optional global leaderboard (Supabase). Leave blank to use on-device scores + challenge links only.
// See README.md → "Global leaderboard" for the one-time setup.
window.GM_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
};
